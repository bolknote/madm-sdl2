# Baby-like programs (non–Williams-tube bit packing)

Runnable on MADM as normal `programs/*.store` where noted; **hex dumps** here use the
packing from the original FPGA/book tooling, not LSB-left tube order.

| File | MADM store | Source |
|------|------------|--------|
| `blackice_multiply.lines.hex` | `programs/blackice_multiply.store` | [BlackIce MX book](https://lawrie.github.io/blackicemxbook/Soft_Processors/Soft_Processors.html) |
| `blackice_multiply.disasm` | (same algorithm) | Book listing |

Canonical copies also live under `reference/blackice_multiply.*`.
